import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.listadecompras',
  appName: 'listadecompras',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
